# Agent Instructions

When changing this project, keep `.agents/skills/` accurate.

If a change affects the public API, setup requirements, runtime behavior, recommended workflow, constraints, or examples described by a skill, update the affected `SKILL.md` in the same change. Do not update skills for unrelated internal changes.
